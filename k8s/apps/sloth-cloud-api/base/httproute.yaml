apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sloth-cloud-api
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: traefik
  hostnames:
    - api.lab.localhost
  rules:
    - backendRefs:
        - name: sloth-cloud-api
          port: 80
