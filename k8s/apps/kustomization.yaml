apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Keep this root empty by default.
# Lab overlays must be added explicitly only after image, secrets, and rollback
# checks are complete, so no placeholder service is pulled into Argo CD by accident.
