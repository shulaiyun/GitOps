apiVersion: v1
kind: Service
metadata:
  name: sloth-cloud-api
  labels:
    app.kubernetes.io/name: sloth-cloud-api
spec:
  selector:
    app.kubernetes.io/name: sloth-cloud-api
  ports:
    - name: http
      port: 80
      targetPort: http
